# ManboHakimi-Harp 用户手册（详细版）

> 三角洲行动口风琴**纯视觉**练习辅助浮窗 · 详细使用手册  
> 适用版本：v0.14.1 · 对应产物 `dist/ManboHakimi-Harp.exe`（约 46 MB）
>
> **v0.14.1 判定手感修复**：修掉「第一次没按好、再按还是 MISS」「长按整条判 MISS」「键缝空按也断连击」。
> 现在长音提前压住算有效、抢拍/拖拍记一次 MISS 但不消耗音符（可以重按救回）、附近没音符的空按静默忽略。详见 §14.6。
>
> **v0.14 曲目侧边栏**：曲目列表不再挂在设置面板里，改为浮窗**左侧的悬停抽屉**——
> 鼠标移上去展开、移开自动缩回，收起后仍留一条 22px 的竖条把手。详见 §5.7 与 §7。
>
> **v0.13 更名说明**：项目由原名 **HarpGuide** 更名为 **ManboHakimi-Harp**。
> EXE 文件名、窗口标题、托盘名称、文档标题均已同步；旧的 `HarpGuide.exe` 可以删除。
> 若你原来把文件放在 `%APPDATA%\HarpGuide\`，首次运行新版会自动把配置与曲目迁移过来（EXE 同级目录便携模式不受影响）。

---

## 目录

1. [这是什么 / 不是什么](#1-这是什么--不是什么)
2. [风险声明与免责](#2-风险声明与免责)
3. [安装与首次启动](#3-安装与首次启动)
4. [快速上手（5 分钟）](#4-快速上手5-分钟)
5. [主界面怎么读](#5-主界面怎么读)
6. [模式切换](#6-模式切换)
7. [曲目选择与切换](#7-曲目选择与切换)
8. [A-B 段落循环](#8-a-b-段落循环)
9. [进度条跳转](#9-进度条跳转)
10. [长按音符提示体系](#10-长按音符提示体系)
11. [琴键校准（多分辨率）](#11-琴键校准多分辨率)
12. [乐谱编辑器](#12-乐谱编辑器)
13. [设置面板全解](#13-设置面板全解)
14. [热键全表](#14-热键全表)
15. [退出程序](#15-退出程序)
16. [数据文件位置](#16-数据文件位置)
17. [高 DPI 与多显示器](#17-高-dpi-与多显示器)
18. [自定义曲目（手写 JSON）](#18-自定义曲目手写-json)
19. [常见问题](#19-常见问题)
20. [故障排查](#20-故障排查)

---

## 1. 这是什么 / 不是什么

**是什么**

- 一个**只读**的视觉辅助浮窗：在屏幕上画"现在该按哪个键、什么时候按、按多久"。
- 你跟随提示**手动**按键盘，工具不参与弹奏。

**不是什么**

- ❌ 不模拟任何键鼠输入
- ❌ 不读取游戏画面或内存
- ❌ 不注入游戏进程
- ❌ 不联网，数据完全本地存储
- ❌ 不是外挂、不是自动弹琴、不是宏

---

## 2. 风险声明与免责

游戏官方使用内核级反作弊系统（ACE），对第三方 Overlay 的合规边界未明确表态，  
**存在被检测的可能性**。首次启动会弹出风险声明，须勾选确认后方可使用。

**建议**：

1. 先在训练场测试（不进入匹配 / 排位）
2. 用风险较低的账号（不要拿主账号冒险）
3. 如官方明确禁止此类工具，请立即停止使用

勾选"我已了解风险"即视为同意自行承担使用后果。

---

## 3. 安装与首次启动

### 安装

ManboHakimi-Harp 是**单文件绿色版 EXE**，无需安装：

```
1. 解压任意目录（如 D:\Tools\ManboHakimi-Harp）
2. 双击 ManboHakimi-Harp.exe
3. 阅读并勾选风险声明 → 进入主界面
```

### 首次启动

首次运行会在 EXE 同级目录创建：

```
ManboHakimi-Harp.exe
config/
  settings.json      # 配置（变速、浮窗位置、A-B 区间…）
  calibration.json   # 校准数据（按"分辨率@缩放比"分档）
scores/              # 用户曲目（空，可放入自制的 .json）
```

如果 EXE 同级目录不可写（例如 `Program Files\`），会自动回退到  
`%APPDATA%\ManboHakimi-Harp\`。

---

## 4. 快速上手（5 分钟）

1. **启动游戏**，让游戏进入**训练场**或任意能听到口风琴的界面。
2. **运行 ManboHakimi-Harp.exe**，阅读风险声明并勾选确认。
3. 浮窗出现在屏幕中央，可能挡住游戏键位 → **拖动顶部蓝条**把它挪到不挡视线的位置（推荐放在屏幕下方或侧面）。
4. **换曲**：鼠标移到浮窗**左边缘的竖条把手**上，曲目抽屉会滑出来，点一首即可（选完自动缩回）。
5. 按 **F1** 开始 / 暂停；按 **F4** 切到悬浮球模式（更不挡视线）。
6. 跟着瀑布流的提示**手动**按 Z X C V B N M , 这 8 个键。
7. 练完按 **Ctrl+Shift+Q** 退出。

> 第一次用建议先选《小星星》《欢乐颂》这类 4 拍简单曲目；流行曲谱面是简化版，可能与原曲略有出入。



---


## 5. 主界面怎么读

```
┌─ ManboHakimi-Harp 浮窗（无边框，可拖动，可置顶） ────────────────┐
│ ● ♪ 小星星              BPM 90      1.0x     ⚙           ← 顶部条（拖动+设置入口） │
├─────────────────────────────────────────────────────────────┤
│▐  ·           ·           ·           ·           ·   ← 预备拍（橙色圆点） │
│▐                                                              │
│▐  ┌──┐        ┌──┐                                          │
│▐  │  │        │  │  ← 瀑布流：音符从顶部下落               │
│▐  └──┘        └──┘                                          │
│▐     ·                                                       │
│▐        ════════════                                         │
│▐        ┌─────────┐                                          │
│▐        │   Z     │  ← 判定线：音符头部压线时按下          │
│▐        └─────────┘                                          │
│▐                                                              │
│▐                                                    ← 曲目侧边栏把手（v0.14） │
├─────────────────────────────────────────────────────────────┤
│ Z  X  C  V  B  N  M  ,                                       │
│ ▢  ▢  ▢  ▢  ▢  ▢  ▢  ▢  ← 琴键提示区（七态机）              │
├─────────────────────────────────────────────────────────────┤
│   一闪一闪亮晶晶，满天都是小星星           ← 歌词提示区（随进度推进） │
├─────────────────────────────────────────────────────────────┤
│ ████████░░░░░░░░░░░░░░░░░░░░░░░░░░░  1:23 / 3:45  ← 进度条（可点击跳转） │
└─────────────────────────────────────────────────────────────┘
```

### 5.1 顶部信息条

| 元素       | 含义             |
| -------- | -------------- |
| `♪ 曲名`   | 当前曲目；首字母为音符符号  |
| `BPM 90` | 当前曲目每分钟节拍数     |
| `1.0x`   | 播放速率（变速滑杆的当前值） |
| `⚙`      | 打开设置面板         |

### 5.2 瀑布流（核心提示区）

- 音符从顶部向判定线**下滑**，到判定线那一瞬间**按下对应键**
- 短按音符（tap）：固定高度方块，**头部压线**时点击
- 长按音符（hold）：**长条**，高度 = 持续时长；头部压线按下，**按住不放**，直到尾部菱形移过判定线

### 5.3 琴键提示区

最下面那 8 个方块，对应 Z X C V B N M ,。它们会随着瀑布流联动地变化状态：

| 状态   | 含义                 | 视觉                     |
| ---- | ------------------ | ---------------------- |
| 待机   | 默认灰青色              | ▢                      |
| 预亮   | 这个音符即将到来（提前 N 拍）   | 边框呼吸闪烁                 |
| 下一个  | 下一个音符（即将压线）        | 边框白色实亮                 |
| 短按中  | 短按音符头部正在判定线        | 实心高亮 + 旋转光环            |
| 长按中  | 长按音符按住             | 实心高亮 + 底部**绿色剩余时间进度条** |
| 接近松开 | 长按尾部接近判定线（剩余 <20%） | 整体**渐变橙色**             |
| 已弹过  | 已越过判定线             | 绿色淡入渐隐                 |

### 5.4 进度条

显示当前位置 / 总时长。

- **可点击**：单击任意位置跳转到该时间点
- **可拖动**：按住左键拖动跳转
- **A/B 手柄**：设置了 A-B 循环时，进度条上会画两个橙色手柄和中间的蓝色循环区间
- **右下角时间**：`已播 / 总时长`（mm:ss 格式）

### 5.5 歌词提示区（v0.7）

位于琴键区与进度条之间，随播放进度自动推进当前歌词行：

- 当前行居中、主青色、大字号；下一行淡出预览
- 18 首歌曲类内置曲目已配歌词；练习曲/纯音乐没有歌词时，歌词区自动隐藏
- 设置面板「显示歌词提示」开关可关闭

### 5.6 调整窗口大小（v0.7）

浮窗可自由缩放：

- **拖拽任意边缘或四角**改变宽高（光标自动切换）
- 右下角有斜线手柄提示
- 瀑布流随窗口高度弹性伸缩；宽度变化时已校准琴键按比例缩放
- 尺寸自动保存，下次启动恢复
- 鼠标穿透开启时边缘拖拽失效（先 Ctrl+Shift+T 关穿透再调）

### 5.7 曲目侧边栏（v0.14）

浮窗**左边缘**常驻一条 22px 宽的竖条把手，是曲目列表的抽屉入口：

```
┌─ 顶部信息条 ────────────────────────┐
│                                     │
│▐ ← 把手（竖排"曲目"）                │  ← 鼠标移上去 → 抽屉滑出
│▐   顶部：曲目总数                    │     移开鼠标   → 自动缩回
│▐   底部：▶ 展开 / ◀ 收起             │
│                                     │
└─────────────────────────────────────┘
```

- **展开**：鼠标碰到把手立刻滑出（170ms 缓动），露出可滚动的曲目列表
- **缩进**：鼠标移开 450ms 后自动收回，**只留把手**——所以它不会一直占着画面
- 把手纵向范围**避开顶部信息条与底部热键提示条**：展开时照样能拖动窗口、点齿轮，
  底部那排热键键帽说明也不会被挡住
- 展开时抽屉会临时盖住左侧的瀑布流、琴键与进度条左段（抽屉的常规行为）；缩回后完全让开
- 点选曲目后抽屉会立刻收起，选完就走
- 把手那一段的左边缘**让给了抽屉**（不再触发窗口左边缘的拖拽缩放），否则鼠标会被系统
  当成"压在边框上"而收不到悬停。想调宽度请拖**其他三边**，或拖左边缘把手上下两端的空白段

> 把手被覆盖到一点内容也没关系：默认窗口宽度（640）下，第一个琴键从 x≈29 开始，
> 22px 的把手不碰任何琴键或音轨列。

---

## 6. 模式切换

ManboHakimi-Harp 有 3 种显示模式：

| 模式           | 触发        | 适合场景              |
| ------------ | --------- | ----------------- |
| **完整模式**（默认） | 启动 / 按 F4 | 练习 + 看所有提示        |
| **悬浮球**      | 按 F4      | 不挡视线时只看"下一个键"     |
| **隐藏**       | 按 F3      | 完全不显示（不暂停播放，节省屏幕） |

- **完整模式 → 悬浮球**：按 F4；浮窗收起成右下角小圆球，显示当前应按字符
- **悬浮球 → 完整模式**：点悬浮球 / 按 F4
- **隐藏 / 取消隐藏**：按 F3 切换；隐藏时 F1 / F2 / A-B 等与播放相关的热键会被忽略（避免按了却看不到进度）

### 系统托盘（Windows 任务栏右下角）

启动后**任务栏右下角会出现 ManboHakimi-Harp 图标**（与 EXE 图标一致）。

- **左键单击**：切换浮窗显示 / 隐藏
- **右键菜单**：
  - 显示 / 隐藏浮窗
  - 设置面板…
  - 开始 / 暂停（带 ✓ 勾选状态，自动同步引擎播放状态）
  - 重置到开头
  - 琴键校准…
  - 乐谱编辑器…
  - 刷新曲库（手工放进 `scores/` 的 JSON，刷一下就能看到，不必重启）
  - 打开曲目文件夹…（直接定位到 `scores/` 目录）
  - **退出 ManboHakimi-Harp**

> 这是最符合 Windows 习惯的"后台工具管理"方式。建议把托盘图标固定在任务栏上（Windows 默认收纳在折叠区，可右键托盘箭头 → "设置" → 始终显示）。
> Linux 服务器 / Wayland 等无托盘环境会自动降级为仅浮窗操作，仍可用 Ctrl+Shift+Q 退出。

---

## 7. 曲目选择与切换

### 通过左侧边栏（v0.14 起，推荐）

曲目列表住在浮窗**左边缘的抽屉**里：

1. 鼠标移到浮窗左边缘那条竖条把手上（写着竖排「曲目」、顶部是曲目数量、底部有个 `▶`）
2. 抽屉**自动滑出**，露出完整曲目列表（可滚动）
3. 点选任意一首 → 切换立即生效，抽屉**自动收回**
4. 鼠标移开也一样会缩回（450ms 后），不用手动关

把手上的信息：

| 位置 | 含义 |
|------|------|
| 顶部数字 | 当前曲库曲目总数 |
| 中间竖排字 | 「曲目」，提示这里能点开 |
| 底部箭头 | 收起时 `▶`（向右展开）；展开时 `◀`（向左收起） |

### 曲目右键菜单

在侧边栏的曲目项上**右键**：

- **在编辑器中编辑**：切到该曲目并用编辑器打开
- **重命名**：改名并自动迁移文件 ID
- **删除**：内置曲目**不可删**（删除文件不存在，会提示）；用户曲目可删，**会二次确认**

### 什么情况下抽屉会自己收起来

- 鼠标移开抽屉（延时 450ms）
- 点选曲目之后（选完就走）
- 打开**鼠标穿透**后（穿透态窗口收不到鼠标事件，抽屉会撤下，关掉穿透自动回来）
- 进入**琴键校准**时（避免挡住第一键的拖拽）

### 自动恢复上次曲目

启动时会自动加载上次选择的曲目（写入 `config/settings.json`）。

---

## 8. A-B 段落循环

> 这是 v0.6 的核心功能。反复练同一段难点，不再每次从头来。

### 用法（最快上手）

1. 播放（F1）到你**想循环的起点**按 **Ctrl+Shift+A** → A 点设好
2. 继续播放到**终点**按 **Ctrl+Shift+B** → B 点设好，**循环自动开启**
3. 练习！播放头到 B 点会**自动跳回** A 点
4. 想取消：再按一次 **Ctrl+Shift+B**（清除）或设置面板点"清除区间"

### 标记位置

- **瀑布流顶部**：橙色 "A" 标记线
- **瀑布流顶部下方**："B" 标记线
- **两者之间**：蓝色半透明高亮 = 当前循环区间
- **进度条**：两个橙色三角形手柄 + 中间蓝色区间

### 设置面板里也能设

如果当前在编辑曲目、想精确到拍：

1. 打开设置面板 → "A-B 循环"分组
2. 在 "A 拍" / "B 拍" 输入框填入拍数（例如 8.0 / 16.0）
3. 点"应用"

### 一键圈一段（无 A 点时按 B）

如果只按了 B 点（没按 A 点），**B 点按下时 A 点会自动吸附到曲首**——便于"快速圈后段"。

### 按曲目分别保存

A-B 区间按曲目 ID 写入 `config/settings.json`：

```json
{
  "loop_ranges": {
    "loner": [16.0, 24.0],
    "butterfly": [4.0, 12.0]
  }
}
```

下次切回同一曲目会自动恢复。

### 切换曲目自动清

切到另一首时，A-B 区间会**自动清除**，避免循环区间不对应新曲目造成奇怪体验。

---

## 9. 进度条跳转

进度条在浮窗底部，是一个蓝色填充的横条。

- **点击**：跳转到对应位置（保留当前播放状态）
- **拖动**：按住左键拖动到目标位置后释放
- **A-B 跳转**：如果拖到 A-B 区间外，播放头会被引擎**拉回 A 点**（避免区间失效）
- **不暂停**：跳转不影响播放/暂停状态

---

## 10. 长按音符提示体系

ManboHakimi-Harp 的核心交互。**瀑布流与琴键联动**呈现一个音符的全生命周期：

### 时序

| 时机      | 瀑布流                | 琴键区                 |
| ------- | ------------------ | ------------------- |
| 提前 N 拍  | 长条从顶部下落            | **边框呼吸闪烁**（预亮）      |
| 到达判定线   | 头部白色亮条压线           | **实心高亮 + 旋转光环**     |
| 长按中     | 长条持续外发光            | 键底**绿色剩余时间进度条**从满到空 |
| 剩余 <20% | 整条**渐变为橙色**，尾部菱形闪烁 | 键位变橙 + 进度条变橙闪烁      |
| 结束      | 音符消失               | 绿色淡入渐隐（已弹过）         |

> 提前量 N 由设置面板的「长按预亮提前量」控制，默认 2 拍。

### 短按 vs 长按

- **短按**（tap）：固定高度方块，头部压线**瞬间点击**即可
- **长按**（hold）：长条；按住的开始时机 = 头部压线时；按住的结束时机 = 尾部菱形移过判定线时（**实际弹奏中不需要严格等到尾部，工具只负责"告诉你什么时候按"**）

---

## 11. 琴键校准（多分辨率）

> 不同分辨率/窗口模式下，游戏内键位大小与间距会变。校准模式可让提示精确对齐到游戏键位。

### 什么时候需要校准

- 换显示器 / 改分辨率后
- 改 Windows 缩放比例（100% / 125% / 150%）后
- 第一次使用时（瀑布流音轨与游戏键位**对不齐**就一定要校）

### 步骤

1. 把游戏调到你**实际要玩**的窗口模式（窗口 / 无边框 / 全屏）
2. **不**启动游戏进入匹配，进入训练场或任意能听到口风琴的界面
3. 按 **Ctrl+Shift+C** 进入校准（播放自动暂停，鼠标穿透自动关闭，悬浮球模式会切回完整模式）
4. **拖动浮窗顶部蓝条**，把整个浮窗大致放在游戏口风琴键位上方
5. 单独**拖动**每个琴键提示（按 `Z X C V B N M ,` 对应）到游戏键位的中心
6. **滚轮**缩放单个琴键的大小（如果游戏键位比提示框大 / 小）
7. **方向键**微调 1 像素（按住 `Shift` = 10 像素）
8. 顶部"整体放大 / 缩小"按钮可一键缩放全部 8 个键
9. 校准期间瀑布流会显示**青色虚线**，指示每个琴键的中心 → 实时确认对齐
10. 满意后点 **保存并退出**

### 面板按钮

- **保存并退出**：写入 `config/calibration.json`（按"分辨率@缩放比"分档）
- **放弃修改**：恢复到磁盘上保存的方案（不保存当前修改）
- **恢复默认网格**：回到内置的均匀 8 等分网格
- **整体放大 / 缩小**：全部 8 个键同比例变大或缩（每次 ±5%）

### 多显示器

浮窗被拖到另一块屏时（且该屏分辨率/缩放比不同），**自动切换**到该屏的校准档案；切回原屏再切回去。  
如果新屏没有档案，会回到默认网格（不会套用其他屏的像素偏移导致错位）。

### 高 DPI

校准按"物理分辨率 @ 缩放比"分档——同一块屏在 100% / 125% / 150% 缩放下分别独立保存，  
换缩放后旧校准不会被错位套用。

---

## 12. 乐谱编辑器

> 按 **Ctrl+Shift+E** 打开钢琴卷帘式编辑器，不需要手写 JSON 也能做自己的曲子。

### 界面

```
┌─ 乐谱编辑器 ─────────────────────────────┐
│ 曲名: [我的曲子       ]   BPM: [120   ]   │
│ [打开] [保存并载入] [试听] [撤销][重做]  │
│ [复制] [剪切] [粘贴] [删除] [全选] [清空] │
│ [粘贴简谱] [导入 JSON] [导出 JSON]       │
├──────────────────────────────────────────┤
│ 1' ──┐                       ┌──┐        │ ← 行 = 8 个键
│ 7   ─┤                      │  │        │   (顶部=高音1)
│ 6   ─┤                     ┌┘  └┐        │
│ 5   ─┤                    ┌┘    └┐       │
│ 4   ─┤                   ┌┘      └───┐   │
│ 3   ─┤                  ┌┘          └──┐│
│ 2   ─┤        ┌──┐──────┘              ││
│ 1   ─┴───┐┌───┘└──┘                    │
└──────────────────────────────────────────┘
       ^判定线                ^时间→
```

### 操作

| 操作                             | 效果                          |
| ------------------------------ | --------------------------- |
| 左键点空白                          | 新建 1 拍短按音符                  |
| 按住空白向右拖                        | 拉长时值；**超过 1 拍自动转长按**        |
| 左键拖动音符                         | 移动起始位置（按 0.5 拍吸附）           |
| 按住音符右缘拖动                       | 调整时值                        |
| 右键点音符                          | 删除                          |
| `Ctrl+Z` / `Ctrl+Y`            | 撤销 / 重做（多步历史）               |
| `Ctrl+C` / `Ctrl+X` / `Ctrl+V` | 复制 / 剪切 / 粘贴（粘贴按 0.5 拍网格落点） |
| `Ctrl+A`                       | 全部选中                        |
| `Del` / `Backspace`            | 删除选中                        |

### 粘贴简谱（v0.15）

从网上抄到一段数字简谱，不需要一个音一个音点——点工具栏「**粘贴简谱**」，粘进对话框：

```
| 1 1 5 5 | 6 6 5- | 4 4 3 3 | 2 2 1- |
```

- 对话框**边打字边报数**：识别了多少音符、大约多长；**认不出来的写法会单独列出来**
  （比如把 `Q`、小写 `z`、全角符号抄了进去）。这些原本会被静默丢掉，现象是"少了一个音"
- **全角数字 / 顿号 / 逗号自动转半角**——`１、２，３` 与 `1 2 3` 等价
- 勾「**追加到现有内容末尾**」= 接在现有音符后面（自动对齐到整拍）；不勾 = **替换**编辑区
- 两种方式都**可以 Ctrl+Z 撤销**
- 改了「曲名」再保存 = 存成**新曲目**；沿用原曲名 = 覆盖当前在编辑的这首

简谱写法：

| 写法 | 含义 |
| --- | --- |
| `1`–`7` | 音级（对应 `Z`–`M` 键） |
| `8` 或 `1'` | 高音 1（`,` 键） |
| `3-` | 长按 2 拍（每多一个 `-` 再加 1 拍） |
| `-` | 独立延音线：把前一个音符再延长一拍 |
| `0` | 休止 1 拍 |
| `0.5:3` | 八分音符（0.5 拍）的 3 |
| `#4` / `b3` | 升 4 / 降 3（半音，切调性档吹） |
| `|` | 小节线（可省略） |
| `//` | 行注释 |

### 保存

1. 顶部改"曲名"和"BPM"
2. 点"保存并载入"→ 写入 `scores/<曲名>.json`（自动 sanitize 文件名）→ 浮窗曲目列表刷新并**切换到新曲目**

### 试听

1. 点"试听" → 编辑中的内容（**未保存**也会播放）直接载入浮窗
2. 再按一次"试听"按钮暂停
3. 试听不影响编辑；退出试听前不会保存

### 内置 vs 用户曲目

- 编辑器保存的曲目写入 EXE 同级的 `scores/` 目录
- 与 26 首内置曲目**合并显示**
- 同 id 时**用户版本覆盖内置**（如需恢复内置版本，删除用户文件即可）

---

## 13. 设置面板全解

打开方式：浮窗顶部右上角 ⚙ 按钮。

### 13.1 曲目

**v0.14 起曲目列表已迁出设置面板**，不再需要点齿轮才能换曲。面板这里只留一段指路说明。

曲目现在住在浮窗**左侧边栏**：鼠标移到左边缘的竖条把手上自动展开，移开自动缩回。
完整说明见 [§5.7 曲目侧边栏](#57-曲目侧边栏v014) 与 [§7 曲目选择与切换](#7-曲目选择与切换)。

右键菜单（在侧边栏的曲目项上右键）：

- 在编辑器中编辑
- 重命名
- 删除（用户曲目可删；内置曲目**不可删**）

### 13.2 A-B 循环

- 设 A 点（当前位置）/ 设 B 点并循环 / 清除
- 直接输入拍数（"A 拍" / "B 拍" 输入框 + 应用）

### 13.3 演奏

| 项       | 范围         | 默认   | 说明                             |
| ------- | ---------- | ---- | ------------------------------ |
| 变速      | 0.25x – 2x | 1.0x | 调整瀑布流下落速率（不改变音高/音速，只是把节奏放慢/加快） |
| 整曲循环    | 开/关        | 关    | 到末尾自动回到曲首（不计预备拍）               |
| 自动变调    | 开/关        | 关    | 半音阶口风琴：调性档自动跟随歌曲，免手动小键盘切调（见 §14.5） |
| 预备拍数    | 0 – 8      | 4    | 曲首前几拍橙色倒数（4 表示 1 2 3 4 然后开始）   |
| 长按预亮提前量 | 0 – 4 拍    | 2.0  | 琴键边框呼吸闪烁从何时开始                  |

### 13.4 显示

| 项     | 范围            | 默认   | 说明                    |
| ----- | ------------- | ---- | --------------------- |
| 不透明度  | 0.3 – 1.0     | 0.82 | 浮窗整体透明度（设到 1.0 看得最清楚） |
| 瀑布流高度 | 100 – 600 px  | 220  | 顶部蓝条到底部琴键的距离（也可直接拖拽窗口边缘调整） |
| 下落时间  | 800 – 4000 ms | 2000 | 音符从顶部到判定线的时长（越大越慢）    |
| 拍线    | 开/关           | 开    | 瀑布流内每个节拍的横线           |
| 歌词提示  | 开/关           | 开    | 琴键区下方随进度推进的歌词行        |
| 命中反馈  | 开/关           | 开    | 演奏时给出 PERFECT/GREAT/GOOD/MISS 判定、连击、得分（见 §14.6） |
| 判定偏移  | -600 – +600 ms | 0    | 平移判定基准，补偿「整体按早/按晚」；详情见 §14.6.2 |
| 底部热键提示条 | 开/关           | 开    | 浮窗最下方常驻热键提示，宽度不足时自动换行 |
| 长按流光  | 开/关           | 开    | 长按音符外侧的发光动画           |
| 松开预警  | 开/关           | 开    | 剩余 <20% 时整条变橙 + 菱形闪烁  |

### 13.5 鼠标穿透

- 全局开关：开启后浮窗**完全透明于鼠标**，游戏不会失焦
- 拖动浮窗 / 操作浮窗按钮需要**先关穿透**

### 13.6 热键冲突告警

启动时检测每个热键是否被其他程序占用。被占用时此处出现黄色告警条，列出冲突的热键名。

**处理方式**：

1. 关掉占用该热键的程序（最常见：QQ / 微信 / OBS / VS Code 等都爱占 F1–F4）
2. 或修改 `harpguide/hotkeys.py` 的 `HOTKEY_DEFS`，换其他组合后**重新打包** EXE

---

## 14. 热键全表

| 热键               | 功能                            | 备注                      |
| ---------------- | ----------------------------- | ----------------------- |
| **F1**           | 开始 / 暂停提示                     | 也用作显示/隐藏模式下被忽略（避免看不到进度） |
| **F2**           | 重置到 A 点（无 A-B 时回曲首）           |                         |
| **F3**           | 显示 / 隐藏浮窗                     |                         |
| **F4**           | 切换悬浮球 / 完整模式                  |                         |
| **Ctrl+Shift+A** | 把当前位置设为 **A-B 循环的 A 点**       |                         |
| **Ctrl+Shift+B** | 把当前位置设为 **B 点** 并开启循环；再按 = 清除 |                         |
| **Ctrl+Shift+T** | 切换鼠标穿透                        |                         |
| **Ctrl+Shift+C** | 琴键校准模式（已在校准时再按 = 保存退出）        |                         |
| **Ctrl+Shift+E** | 乐谱编辑器（开关）                     |                         |
| **Ctrl+Shift+Q** | 退出程序                          | 3 种退出方式见 §15           |
| **小键盘 1**      | 切「降调」档（半音阶口风琴 -1 半音）         |                         |
| **小键盘 2**      | 切「半音/自然」档（回到自然音阶）            |                         |
| **小键盘 3**      | 切「升调」档（半音阶口风琴 +1 半音）         | 见 §14.5 半音阶口风琴        |
| **小键盘 0**      | 开 / 关「自动变调」（跟随歌曲自动切调）         | 开启后调性档自动跟随，免手动切调   |
| **小键盘 4 / 6**  | 判定偏移 **-20 / +20ms**（补偿按早 / 按晚） | 见 §14.6.2 判定偏移          |
| **小键盘 5**      | 判定偏移归零                        |                         |

> 仅 Windows 生效；其他平台静默降级（无热键）。  
> 启动时若有冲突，设置面板顶部会列出告警。

---

## 14.5 半音阶口风琴（v0.8 起，v0.9 加自动变调）

游戏里的口风琴带「降调 / 半音 / 升调」三个推键（鼠标左键 / 中键 / 右键），本质是**半音阶口风琴**：
8 个物理键 + 推键切半音，能吹出 12 个半音。ManboHakimi-Harp 已完整同步这套机制。

**怎么用（手动模式）**：

1. 谱面遇到半音音符（简谱里的 `#4`、`b3` 等），该**琴键右上角**和**瀑布流音符**上会画一个橙色 `#` 标记
2. 顶部信息条显示当前调性档，若和谱面需要的不符，会橙色提示「**→ 需升调↑**」
3. 按 **小键盘 3** 切「升调」，同时游戏里用鼠标右键切「升调」；然后按对应物理键吹出半音
4. 吹完半音，按 **小键盘 2**（游戏里鼠标中键）切回「自然」档

**自动变调（v0.9，推荐）**：

不想手动记「什么时候切档」，就开启自动变调——调性档会**自动跟随歌曲**，玩家只专注按 8 个物理键即可。

- 开启：按 **小键盘 0**，或设置面板「演奏」区勾选「**自动变调**」
- 开启后顶部信息条变**绿色**，显示「自动变调 · 升调↑ / 自然♮ / 降调↓」，即当前这一拍该用的档
- 物理键仍按琴键/瀑布流上的高亮与 `#` 标记来按；游戏里仍要同步用鼠标右键/中键/左键切到提示的档（ManboHakimi-Harp 不模拟键鼠，只做提示）
- 手动按 **小键盘 1/2/3** 会自动退出自动变调，回到手动模式

**三档对应关系**：

| ManboHakimi-Harp 热键 | 调性档 | 游戏鼠标键 | 效果 |
|--------------|-------|-----------|------|
| 小键盘 1 | 降调 | 左键 | 8 键整体降半音 |
| 小键盘 2 | 半音/自然 | 中键 | 自然音阶（默认） |
| 小键盘 3 | 升调 | 右键 | 8 键整体升半音 |

**记谱规则**（半音阶口风琴单推键习惯）：半音统一用「升调档」吹出。简谱里 `#1 #2 #4 #5 #6` 是升号；
`b2 b3 b5 b6 b7` 等降号会**自动等价转成升号**（如 `b3` = `#2`，都吹 X 键 + 升调档）；
`b1`=7、`b4`=3、`#3`=4、`#7`=高音1 这类等价自然音直接按自然键，无需推键。

> 内置曲库有一首「**练习·半音阶上下行**」，覆盖 12 个半音，选它就能立刻体验推键吹半音。

---

## 14.6 命中反馈（v0.10 起，v0.14.1 判定手感重做）

类似《指上钢琴》等音游的**判定 / 连击 / 得分**系统。

**开启**：设置面板「显示」区勾选「**命中反馈**」，默认开启。

> 设计原则：**这是练习器，不是裁判。** 按错了要提醒，但不能罚得让人不敢练。

**判定规则**（按按键时刻与音符起点的差距）：

| 判定 | 时间窗口 | 颜色 | 基础分 |
|---|---|---|---|
| PERFECT | ±110 ms | 金 | 100 |
| GREAT | ±220 ms | 绿 | 70 |
| GOOD | ±350 ms | 青 | 40 |
| 抢拍 / 拖拍 | 350 ~ 510 ms | 红（提示一次） | 0 |
| 漏音（起点 +510ms 仍无人按） | — | 红 | 0 |
| 键缝空按（该键附近无音符） | — | **不判 MISS** | — |

- **抢拍 / 拖拍**：偏移超出 GOOD 窗口、但仍在 510ms 宽容带内的按键，说明你确实在打这个音符，只是时机偏了。此时**记一次 MISS 提醒，但不消耗该音符**——同一个音符在窗口内**可以重按救回来**。
- **键缝空按**：附近完全没有该键音符的敲击**静默忽略**，不判 MISS、不断连击（练习时试键、摸键位是常态）。
- **长按**：单独判定，详见下方。

**视觉反馈**：

1. 判定文字（PERFECT/GREAT/GOOD/MISS）从该音轨的判定线向上弹出并淡出
2. 命中的琴键瞬间闪出判定色光环（金色/绿色/青色/红色）
3. 连击数 ≥ 2 时，瀑布流中央显示大号的 `COMBO ×N`，10 连击以上变金色
4. 右上角常驻累计得分
5. 顶部信息条显示最近命中的**平均偏移**（累积 4 次后出现）

**连击机制**：PERFECT / GREAT / GOOD 都会 +1 连击并加分（连击越高单次奖励越多）；漏音、抢拍会中断连击并显示红色 MISS。

**安全说明**：命中反馈只用 `GetAsyncKeyState` **被动读取** 8 个演奏键的按下状态，**不安装键盘钩子、不拦截按键、不模拟输入**，游戏里的按键响应完全不受影响。

### 14.6.1 判定手感修复史

**v0.12（第一次修「按对了还是 MISS」）**

| 之前的问题 | 现在的做法 |
|---|---|
| 判定窗口偏严（±300ms 封顶） | 放宽为 **±110 / ±220 / ±350ms** |
| 一次失误被判**两个** MISS（按键空了记一次，音符滑过又记一次） | 新增「**匹配窗口**」：这一列附近有音符时，迟到 / 重复的按键**静默忽略**，不再叠加 MISS |
| 玩家整体按早或按晚（反应 + 画面延迟） | 新增「**判定偏移**」，把整个判定基准平移对齐 |
| 游戏满载时帧被拖慢，快速轻敲可能漏读 | 轮询同时读 `GetAsyncKeyState` 的按下标志位，**不漏掉帧间的轻敲** |
| 漏音定时器与判定窗口一样宽，会**抢先**把"刚好还能判 GOOD"的音符判成 MISS | 漏音窗口 = 判定窗口 **+ 宽限**（v0.14.1 起统一为 `GRACE_MS = 160ms`） |
| 编辑器「试听」/ 换曲后音符换了一批，旧的"已判定"标记被套到新音符上 | 判定标记改记**音符下标**（原先记 `id(note)`，会踩 Python 对象地址复用），换曲时一并清空 |

**v0.14.1（三个残留问题）**

| 之前的问题 | 为什么 | 现在的做法 |
|---|---|---|
| 第一次没按好，**再按还是 MISS** | 旧逻辑一旦偏移超出 GOOD 窗口就直接把音符吃掉。重按时匹配不到原音符，又因附近有音符被静默忽略——于是「第一次 MISS，第二次还是 MISS」，救不回来 | 拆两档：**GOOD 窗口内**才消耗音符；**GOOD ~ 510ms 宽容带内**只记一次 MISS、**不消耗音符**，窗口内重按仍可正常判定。同一音符只提醒一次，连按不刷屏 |
| **长按整条判 MISS** | 旧逻辑只看「按键边沿」。而长音的**正确手法就是在起点之前先压住**，那一刻没有新边沿，整条长音被漏音判成 MISS | 长音改为「**键处于按住状态** + 与音符区间有重叠」判定：**提前 300ms** 内压住都算有效（提前压住不罚，按 0 偏移计即 PERFECT），晚按给 **120ms** 余量、至少算 GOOD。为此按键轮询额外返回「当前按住集合」 |
| **键缝空按也断连击** | 旧逻辑把「附近没音符的敲击」也算 MISS | 空按**静默忽略**：练习时试键、摸键位是常态，不该被罚 |

### 14.6.2 判定偏移（v0.12，强烈建议做一次）

**原理**：浮窗顶部的 `偏移 +XXXms` 是你最近命中时刻相对谱面的**平均差**。

- `+` = 你整体按**晚**了；`-` = 整体按**早**了
- 把这个数字填进「判定偏移」，判定基准就跟着平移，读数会向 0 收敛

**操作**：

1. 先正常弹一段（至少 4 个音符），看顶部出现的 `偏移 +XXXms`
2. 调整「判定偏移」（设置面板「显示」区滑杆，或游戏中按小键盘）：
   - **小键盘 6** = +20ms（数值往正方向调，用于 `+` 的情况）
   - **小键盘 4** = -20ms（数值往负方向调，用于 `-` 的情况）
   - **小键盘 5** = 归零
3. 反复第 1–2 步，直到顶部读数接近 0（**变绿**表示 |偏移| ≤ 110ms，手感已对齐）

> 例：顶部显示「偏移 +210ms」，说明你习惯性慢 210ms；把「判定偏移」调到 **+210**，再弹一段读数就会掉到 0 附近。
>
> 判定偏移按全局生效（不分曲目）；它只影响 ManboHakimi-Harp 的判定显示，**不影响游戏内任何判定**。

## 14.7 底部热键提示条（v0.11）

浮窗最下方新增常驻提示条，把常用全局热键以「**键帽 + 说明**」形式列出来。

- **默认显示**；在设置面板「显示」区取消勾选「**底部热键提示条**」即可隐藏
- 浮窗宽度不足时**自动换行**；主窗口会同步压缩瀑布流高度，保证提示条完整可见
- 纯展示，**不接收鼠标事件**，不影响窗口拖动，也不影响游戏内的鼠标穿透

提示条里的键帽说明：

| 键帽 | 含义 |
|-----|------|
| C-S-X | Ctrl+Shift+X |
| N0 / N1/2/3 / N4/5/6 | 小键盘 0 / 1/2/3 / 4/5/6 |
| F1-F4 | 功能键 F1 至 F4 |

---

## 15. 退出程序

退出有 **3 种方式**：

1. **托盘菜单退出**（推荐，符合 Windows 习惯）：任务栏右下角 ManboHakimi-Harp 图标 → 右键 → "退出 ManboHakimi-Harp"
2. **热键退出**：`Ctrl+Shift+Q`
3. （不推荐）任务管理器强制结束 → 会丢未 flush 的数据

为什么不能关窗口退出：

- 浮窗是**无边框透明置顶**窗口，本身没有 X 按钮
- 直接用任务管理器结束进程**会让设置/校准/A-B 区间数据丢失**（因为正常退出时会 flush 写入磁盘）

如果不小心用了任务管理器，下次启动可能会**恢复上次未保存的状态**——比如 A-B 区间还在但曲目已切走。

### 异常退出后

1. 重新双击 `ManboHakimi-Harp.exe`
2. 如果弹风险声明 → 勾选
3. 进入主界面即视为正常恢复

---

## 16. 数据文件位置

| 文件                        | 位置                              | 用途                     |
| ------------------------- | ------------------------------- | ---------------------- |
| `config/settings.json`    | EXE 同级（或 `%APPDATA%\ManboHakimi-Harp`） | 变速、浮窗位置、风险声明标记、A-B 区间表 |
| `config/calibration.json` | 同上                              | 校准几何（按"分辨率@缩放比"分档）     |
| `scores/<曲目ID>.json`      | 同上                              | 用户自定义曲目（与内置 26 首合并）    |

### 备份建议

- 切到新电脑：复制整个 `config/` 和 `scores/` 即可保留所有配置 + 用户曲目
- 重置工具：删除 `config/settings.json`（重置所有偏好但保留曲目）或整个 `config/`（完全重置）

---

## 17. 高 DPI 与多显示器

ManboHakimi-Harp 在启动时**显式声明** per-monitor DPI awareness（Windows 10 1903+ / Windows 11）。

效果：

- 系统缩放 125% / 150% / 200% 下，浮窗像素与游戏键位的物理坐标保持一致
- 同一块屏在 100% / 125% / 150% 缩放下分别有独立的校准档
- 浮窗被拖到另一块屏时**自动切换**到对应屏幕的校准档案

### 浮窗被任务栏挡住 / 底部展示不全 / 跑出屏幕

v0.6.2 起：

- **启动时自动检测屏幕可用区域**，如果浮窗会超出屏幕下沿/右沿，会自动上移/左移，保证完整可见
- 如果屏幕高度不足，会自动压缩瀑布流区域，优先保证 8 个琴键完整显示
- 拖动顶部信息条松开后也会自动校正，不会拖出屏幕外
- 如果窗口仍找不到：删除 `config/settings.json` 里的 `window_x`、`window_y`，或整文件删掉恢复默认

### 不支持的情况

- Windows 7 / 8 / 10 1809 以下：DPI 缩放可能由系统二次缩放，导致浮窗看起来略糊（但功能正常）
- 早于 1903 的 Windows 10：可能需要手动开启"替代高 DPI 缩放行为"为"应用程序"

---

## 18. 自定义曲目（手写 JSON）

高级用户可用。在 EXE 同级创建 `scores/`，放入 JSON 乐谱，然后**托盘右键 → 刷新曲库**即可看到
（v0.15 起有了刷新入口，不必重启程序；改过的当前曲目会连带重新载入，播放位置不丢）：

> 不知道 `scores/` 在哪？托盘右键 → **打开曲目文件夹…**。
> 不想手写 JSON？用编辑器的「粘贴简谱」，见 [§12](#12-乐谱编辑器)。

> 键位写错不会报错，但该音符会从瀑布流里消失、永远等不到——`key` 必须是大写的
> `Z X C V B N M ,` 之一，或 `"rest"`。写完建议用编辑器的「导入 JSON」看一眼（简谱写法还会在粘贴时把认不出来的 token 直接点名）。

```json
{
  "version": "2.1",
  "id": "my_song",
  "name": "我的曲子",
  "bpm": 90,
  "timeSignature": "4/4",
  "keyMap": ["Z", "X", "C", "V", "B", "N", "M", ","],
  "notes": [
    { "key": "Z", "type": "tap",  "duration": 1.0, "beat": 0 },
    { "key": "X", "type": "hold", "duration": 2.0, "beat": 1 },
    { "key": "V", "type": "tap",  "duration": 1.0, "beat": 3, "accidental": 1 },
    { "key": "rest", "type": "rest", "duration": 1.0, "beat": 4 }
  ],
  "lyrics": [
    { "time": 0,     "text": "第一句歌词" },
    { "time": 2000,  "text": "第二句歌词" }
  ]
}
```

字段：

- `type`: `tap`（短按） / `hold`（长按） / `rest`（休止，不操作）
- `duration`: 时值（拍）。`0.25` = 十六分，`0.5` = 八分，`1.0` = 四分，`2.0` = 二分
- `beat`: 起始拍数（从 0 开始）
- `key`: 必须出现在 `keyMap` 里（共 8 个：`Z X C V B N M ,`），或 `"rest"` 表示休止
- `accidental`: 可选，半音阶口风琴升降号。`0`（默认）= 自然档，`1` = 升调档（吹 `#`），`-1` = 降调档（吹 `b`）。不写则视为 `0`，老乐谱完全兼容
- `lyrics`: 可选。歌词数组，每条 `time`（毫秒）为出现时间、`text` 为文本；按 `time` 升序排列，随播放进度逐条推进

也可以用**简谱语法**（v0.15 起在编辑器点「粘贴简谱」就能直接生成，不需要开发环境；
以下写法与 `gen_scores.py` 用的是同一个解析器）：

```
| 1 2 3- 4 | 5 5 6-- 7 |
| 0.5:1 0.5:2 1- 3 | 8 7 6- |
| 1- - - |
```

- `3-` = 长按 2 拍
- `6--` = 长按 3 拍
- `0.5:3` = 八分音符的 3（0.5 拍）
- `8` = 高音 1（= `,` 键）
- `0` = 休止 1 拍
- `#4` = 升 4（半音，切升调档吹）、`b3` = 降 3（等价 `#2`）
- `-`（独立延音线）= 把前一个音符再延长一拍
- `// 注释`

---

## 19. 常见问题

### Q：热键 F1 / F2 没反应？

A：很可能被其他程序占用（QQ、微信、OBS、VS Code 都爱占）。设置面板顶部"热键冲突"会列出告警。  
关掉占用程序，或修改 `harpguide/hotkeys.py` 的 `HOTKEY_DEFS` 后**重新打包**。

### Q：瀑布流音轨与游戏键位对不齐？

A：第一次用**必须校准**！按 `Ctrl+Shift+C` 进入校准模式，逐个拖动琴键到游戏键位的中心。  
详见 [§11 琴键校准](#11-琴键校准多分辨率)。

### Q：换了分辨率/缩放比例后偏移严重？

A：进入校准模式**重新保存**一份。校准按"物理分辨率@缩放比"分档，换设置后需要各自重新校准。

### Q：A-B 循环怎么设？

A：[§8 A-B 段落循环](#8-a-b-段落循环)。最简方式：播放到起点按 `Ctrl+Shift+A`，到终点按 `Ctrl+Shift+B`。

### Q：能不能只弹奏不放浮窗？

A：可以。按 `F3` 隐藏浮窗（程序继续在后台运行）。再按 `F3` 恢复显示。

### Q：能不能用 MIDI 键盘？

A：当前**只支持 PC 键盘**的 Z X C V B N M , 这 8 个键（与游戏内一致）。如果游戏支持 MIDI 自定义键位，可以映射到这些键。

### Q：流行曲谱面与原曲对不上？

A：26 首曲目里的流行曲都是"主旋律简化版"——受限于 1-7+高音1 共 8 个键，复杂原调（如《枫》）副歌只取核心乐句。  
v0.8 起已支持半音阶（`#4`、`b3` 等），带升降调的原曲也能用推键吹出来了。**简谱文件即源码 `gen_scores.py`**，可在编辑器里按需微调后存为用户曲目。

### Q：怎么改热键？

A：修改 `harpguide/hotkeys.py` 的 `HOTKEY_DEFS`，然后 `build.bat` 重新打包。

### Q：怎么彻底卸载？

A：删除整个 ManboHakimi-Harp 目录即可，所有数据都在 EXE 同级的 `config/` 和 `scores/` 里。不会写入注册表或系统目录。

### Q：便携模式（U 盘）能用吗？

A：可以。把 ManboHakimi-Harp.exe 和空的 `config/` 放到 U 盘，**首次运行**会自动创建配置。但**部分 U 盘文件系统不支持创建符号链接**，如果回退到 `%APPDATA%`，数据会写到电脑硬盘而不是 U 盘——**退出时记得 Ctrl+Shift+Q，不要直接拔 U 盘**。

---

## 20. 故障排查

| 现象              | 排查步骤                                                                         |
| --------------- | ---------------------------------------------------------------------------- |
| 双击 EXE 没反应      | ① 看是否被杀毒软件拦截；② 看 Windows Defender SmartScreen 警告（点"仍要运行"）                    |
| 弹"风险声明"点确认后立刻退出 | 删除 `config/settings.json` 让风险标记重置                                            |
| 浮窗位置异常（跑到屏幕外）   | 删除 `config/settings.json` 中的 `window_x` / `window_y` 字段；或按住 `Alt+Space` 模拟菜单 |
| 进度条不动           | 检查曲目是否正确加载（设置面板"曲目"列表里有这首吗？）                                                 |
| 校准数据丢失          | 检查 `config/calibration.json` 是否存在；是否是 Windows 权限问题                           |
| 编辑器保存失败         | 检查 `scores/` 目录是否可写（特别是 `%APPDATA%` 回退模式）                                    |
| 启动后界面糊          | 系统缩放问题；详见 [§17 高 DPI](#17-高-dpi-与多显示器)                                       |
| 完全没反应（连窗口都没）    | 启动 `cmd`，cd 到 EXE 目录，运行 `ManboHakimi-Harp.exe` 看控制台报错                               |
| 内存占用过高          | 单进程约 80–120 MB，瀑布流每帧重绘是正常的；如果 >500 MB 看是否有 bug                               |
| 命中反馈「按对了还是 MISS」 | 判定窗口只有几百毫秒，多半是整体按早/按晚。看顶栏 `偏移 +XXXms`，用 **小键盘 4 / 6** 或设置面板「判定偏移」把它调到接近 0（详见 [§14.6.2](#1462-判定偏移v012强烈建议做一次)） |
| 判定偏移调了没反应       | 偏移只影响 ManboHakimi-Harp 的判定显示，不影响游戏；调整后旧的统计会清空，需要重新弹 4 个音符才会出现新读数                        |

如果以上都不能解决，**导出诊断信息**：

1. 启动 `cmd`
2. `cd` 到 EXE 所在目录
3. `ManboHakimi-Harp.exe > output.log 2>&1`
4. 等几秒后 `Ctrl+Shift+Q` 退出
5. 把 `output.log` 发给开发者

---

## 附录 A：内置曲库速查（26 首）

### 入门（7 首）

- `twinkle` 小星星（BPM 90）
- `mary_lamb` 玛丽有只小羊羔（BPM 100）
- `two_tigers` 两只老虎（BPM 92）
- `happy_birthday` 生日快乐（BPM 100）
- `jingle_bells` 铃儿响叮当（BPM 112）
- `ode_to_joy` 欢乐颂（BPM 100）
- `auld_lang_syne` 友谊地久天长（BPM 84）

### 进阶 / 练习（6 首）

- `sky_city` 天空之城（简化版）（BPM 76）
- `exercise_scale` 练习·音阶上下行（BPM 80）
- `exercise_hold` 练习·长按控制（BPM 76）
- `exercise_running` 练习·八分音符跑动（BPM 104）
- `exercise_hold_chain` 练习·连续长按衔接（BPM 70）
- `exercise_chromatic` 练习·半音阶上下行（BPM 80）

### 流行 / 动画（13 首，主旋律简化版）

- `father` 父亲（筷子兄弟）（BPM 76）
- `maple` 枫（周杰伦）（BPM 64）
- `anohana` 未闻花名（secret base 中文版）（BPM 84）
- `miracle` 奇迹再现（迪迦奥特曼）（BPM 150）
- `piggie` 猪猪侠（BPM 132）
- `sheep` 别看我只是一只羊（喜羊羊）（BPM 96）
- `loner` 孤勇者（陈奕迅）（BPM 92）
- `see_you` See You Again（BPM 80）
- `butterfly` Butter-Fly（数码宝贝）（BPM 150）
- `mixue` 蜜雪冰城甜蜜蜜（BPM 104）
- `fairy_tale` 童话（光良）（BPM 68）
- `qifengle` 起风了（买辣椒也用券）（BPM 75）
- `haiyu` 海屿你（马也_Crabbit）（BPM 75）

---

## 附录 B：版本演进

| 版本       | 关键变化                                                                                        |
| -------- | ------------------------------------------------------------------------------------------- |
| v0.1     | 8 键浮窗 + 瀑布流 + 长按提示                                                                          |
| v0.2     | 全局热键 + 风险声明 + 悬浮球                                                                           |
| v0.3     | 简谱解析 + 钢琴卷帘编辑器                                                                              |
| v0.4     | 琴键校准（按分辨率持久化）                                                                               |
| v0.5     | 乐谱编辑器 + 曲目合并加载                                                                              |
| **v0.6** | **A-B 段落循环 · 进度条跳转 · 编辑器撤销/复制粘贴/吸附 · 设置面板高级项 · 曲目管理 · 热键冲突告警 · 高 DPI per-monitor · 22 首曲库** |
| **v0.6.1** | **系统托盘（任务栏图标 + 右键菜单退出/设置/校准/编辑器）** |
| **v0.6.2** | **浮窗启动/拖动后自动适配屏幕可用区域，防止被任务栏裁掉；新增屏幕适配自检断言** |
| **v0.7** | **歌词提示（随进度推进）+ 可自由拖拽调整窗口大小（四边/四角）+ 设置面板歌词开关** |
| **v0.8** | **半音阶口风琴：简谱支持 #/b 半音 + 琴键/瀑布流半音标记 + 小键盘 1/2/3 切调 + 顶部调性指示 + 编辑器半音编辑 + 26 首曲库（含半音阶练习曲）** |
| **v0.9** | **自动变调：小键盘 0 / 设置面板一键开启，调性档自动跟随歌曲切换，专注按 8 键即可；手动切调自动退出自动模式** |
| **v0.10** | **命中反馈：被动读取 8 演奏键按下状态，给出 PERFECT/GREAT/GOOD/MISS 判定 + 连击 + 得分，手感类似《指上钢琴》** |
| **v0.11** | **底部热键提示条：浮窗最下方常驻键帽 + 说明形式的热键提示，宽度不足自动换行；设置面板「显示」区可开关** |
| **v0.12** | **判定手感修复：窗口放宽至 ±110/220/350ms + 640ms 匹配窗口（消除「一次失误两个 MISS」）+ 漏音窗口加 90ms 宽限（消除帧序竞争）+ 判定标记改记下标（消除换曲误判）+ 判定偏移校准（小键盘 4/5/6，顶栏实时显示平均偏移）+ 读取按下标志位不漏轻敲** |
| **v0.13** | **项目更名：HarpGuide → ManboHakimi-Harp（EXE / 窗口标题 / 托盘 / 文档 / spec / 项目目录全部同步）+ 旧 `%APPDATA%\HarpGuide` 数据自动迁移（内部 Python 包名 `harpguide` 保持不变）** |

---

## 附录 C：版权与许可

本工具为**个人项目**，仅供学习交流。

游戏《三角洲行动》、动画 IP（迪迦奥特曼 / 数码宝贝 / 喜羊羊 / 猪猪侠）、  
歌曲（筷子兄弟 / 周杰伦 / 陈奕迅 / Wiz Khalifa / 和田光司等）的版权归各自权利人所有。

本工具**不含**任何受版权保护的音频 / 曲谱原版，**不模拟任何键鼠输入**，  
仅做主旋律的简化视觉提示。请在合法合规的前提下使用。
