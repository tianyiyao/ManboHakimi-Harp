# 项目接管审查（2026-09-25）

## 当前判断

0.15.3 是可供测试的 Windows 候选版，尚不能称为公开发布版。源码静态检查、回归测试、应用离屏自检、单文件构建和打包后自检均在本地通过。离屏自检不验证游戏内浮窗、反作弊兼容、多显示器行为或普通用户权限环境。

## 本轮已修复

- 编辑器保存与 BPM 变化时歌词、拍号、键位映射丢失或错位。
- 曲目、设置、校准数据写入失败时文件损坏；重命名覆盖或丢失曲目。
- A-B 循环越界、刷新漏掉歌词变化、删除其他曲目打断当前播放。
- 损坏数据容错、按键重置、多显示器取消校准、打包资源与错误 DLL 混入。
- 外部曲目 JSON 的 `id` 影响删除或重命名路径。现在载入时以文件名确定曲目身份，文件操作还会拒绝路径型 ID。
- 根据实机反馈，把与游戏冲突的 F1–F4 改为小键盘 7/8/9/加号。
- 浮窗加入常用操作按钮，同时保留快捷键；按钮状态随播放变化。

## 发布前必须完成

1. 在 Windows 10/11 实机按 [交付检查清单](RELEASE_CHECKLIST.md) 完成浮窗、托盘、快捷键、缩放、多显示器和游戏训练场验收，并记录游戏版本。
2. 用户已确认内置 26 首曲谱有公开再分发授权；取得每首曲目的授权方、范围和署名要求，加入仓库说明。核对名称、图标及截图权利。
3. 核对 PySide6、Qt 等随 EXE 分发组件的许可通知，准备对应文本。
4. 在 GitHub 原仓库中恢复正确的 Git 历史并运行 Windows CI。当前工作目录无 `.git`，本机此前连接 GitHub 时被重置，因此本地候选代码尚未提交或上传。
5. 最终构建在干净 Windows 环境复测，并决定签名与发布渠道。当前 EXE 未签名，不能用本地自检替代下载后的验证。

## 后续优化建议

1. **可观察性**：在设置中增加“打开日志目录”和“导出诊断包”，让测试者能附上版本、屏幕缩放和匿名化设置。日志默认留在本地。
2. **曲谱质量**：为内置曲谱建立逐首校对表，记录节拍、歌词时间、半音和授权信息；增加曲目格式校验，避免错误 JSON 进入正式包。
3. **大规模使用**：把发行流程固定为 CI 构建、制品哈希、干净虚拟机验证和分批试用。先收集启动失败与误判反馈，再考虑面向大范围玩家发布。
4. **体验**：用真实玩家的训练场记录评估命中判定偏移、视觉提前量和不同缩放下的键位对齐，再决定默认值或校准引导如何调整。

目前没有远程服务或自动更新。若后续增加，需要单独设计隐私说明、错误上报的选择机制和更新签名校验。
